const test = require("../../../functions/node_modules/firebase-functions-test")();
const sendgrid = require("../../../functions/node_modules/@sendgrid/mail");

it("Dummy test (remove when there are real tests", () => {
  expect(true).toEqual(true)
});
